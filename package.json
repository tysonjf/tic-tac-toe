{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "scripts": {
    "dev:client": "npm run dev --workspace=packages/client",
    "build:client": "npm run build --workspace=packages/client",
    "build:server": "npm run build --workspace=packages/server",
    "install:client": "npm install --workspace=packages/client",
    "install:server": "npm install --workspace=packages/server",
    "dev:server": "docker compose -f docker-compose.yml up",
    "start:server": "docker compose -f docker-compose.prod.yml up --build -d"
  },
  "keywords": [
    "tic-tac-toe"
  ],
  "author": "Tyson",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/client",
    "packages/server",
    "packages/shared"
  ],
  "dependencies": {
    "typescript": "^5.4.5"
  }
}
